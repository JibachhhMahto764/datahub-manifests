apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: datahub
  namespace: datahub

spec:
  ingressClassName: traefik

  rules:
    - host: broadway-datahub.192.168.56.3.nip.io

      http:
        paths:
          - path: /
            pathType: Prefix

            backend:
              service:
                name: datahub
                port:
                  number: 80
